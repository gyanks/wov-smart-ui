
/*

const CvesScanService = (nvdCvesList, scanList) => {
  const   componentExper='/{scanList.componentName}/gi'
  alert(componentExper)
 const    versionExper="/{scanList.version}/"
    const matchedCves = nvdCvesList.filter((cve) => componentExper.test(cve.description.description_data[0].value))
    return matchedCves;

}

export default CvesScanService;
*/